# Site Reliability Engineer tasks

This repo stores two techical problems for an SRE engineer.

The first one is a certificate expiry date checker suript that will help with the task of checking the expiery of a list of HTTPS URLs and produce a metric or an human readable output.

The second one is the infrastructure automation setup of a tipical cloud infrastructure with instances, LBs, RDBMS, etc.
